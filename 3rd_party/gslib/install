#!/bin/bash
set -e

if [ "$1" == "clean" ]; then
  rm -rf lib include gslib/src/*.[ao] 2>/dev/null 
  exit 0
fi

if [ -f ./lib/libgs.a ]; then
  exit 0
fi

if [ ! -d gslib ]; then
  git clone https://github.com/thilinarmtb/gslib.git -b gs_setup
fi

cd gslib
set -x
make -j4 $GSLIB_OPT
set +x
