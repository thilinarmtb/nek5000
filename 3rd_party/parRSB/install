#!/bin/bash
set -e

if [ "$1" == "clean" ]; then
  rm -rf parRSB/build lib include 2>/dev/null
  exit 0
fi

if [ -f ./lib/libparRSB.a ]; then
  exit 0
fi

git clone https://github.com/Nek5000/parRSB.git -b metrics

cd parRSB
set -x
make -j4 $PARRSB_OPT
set +x
